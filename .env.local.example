# Copy to `.env.local` (gitignored) for local dev. All vars are optional.

# Used by next.config.js to set basePath for GitHub Pages deploys.
# Leave EMPTY for local `next dev` / `next build`.
NEXT_PUBLIC_BASE_PATH=
